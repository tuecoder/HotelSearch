services:
  app:
    build: .
    ports:
      - "8080:8080"
    command: uvicorn src.app.streamlit_app:app --host 0.0.0.0 --port 8080
    depends_on:
      - duckling

  duckling:
    image: rasa/duckling:latest
    ports:
      - "8000:8000"
